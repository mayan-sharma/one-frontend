import Layout from '../components/Layout';

import Login from '../components/auth/Login';

const LoginPage = () => {
    return (
        <Layout>
            <Login />
        </Layout>
    );
}

export default LoginPage;